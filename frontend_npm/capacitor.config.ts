import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'plaid_react_quickstart_capacitor',
  webDir: 'build'
};

export default config;
